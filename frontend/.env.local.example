# Supabase Configuration (Client-side)
# Local:  http://127.0.0.1:54321 (via `supabase start`)
# Hosted: https://xxxx.supabase.co (from dashboard → Settings → API)
# Do not paste angle brackets. `https://<project-ref>.supabase.co` is not a valid URL.
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# API Configuration
# Browser calls go to this base + path (e.g. /api/brands → FastAPI /brands via rewrite).
# Default in code is /api if unset. On Vercel you MUST set this at build time, or use /api
# and understand Vercel has no FastAPI — point it at your Render app instead, e.g.
# NEXT_PUBLIC_API_URL=https://your-service.onrender.com/api
NEXT_PUBLIC_API_URL=/api
NEXT_SERVER_API_URL=http://127.0.0.1:8000
# Optional alias used by server-side fetches. Defaults to NEXT_SERVER_API_URL.
# BACKEND_INTERNAL_URL=http://127.0.0.1:8000

# Signups gate. Set to "false" in production builds to disable /signup.
# When false, the signup page renders a closed-state card and never calls supabase.auth.signUp.
# Docker production default is false. Local Next.js default (unset or true) keeps the form.
NEXT_PUBLIC_SIGNUPS_ENABLED=true